// let top = document.querySelectorAll('.top');

// top.forEach(function(top) {
//     top.addEventListener('click', topClicked);

// });

// function topClicked(e) {
//     // alert(e.target.textContent);
//     e.target.textContent = 'Hello!';
// };



// let center = top = document.querySelectorAll('.center');

// center.forEach(function(center) {
//     center.addEventListener('click', centerClicked);

// });

// function centerClicked(e) {
//     // alert(e.target.textContent);
//     e.target.textContent = 'Hello!';
// };

// myBox = ['cheese', 'sneeze', 'freeze', 'please'];
// var f = 0;
// while (f < 3){

// }

let row = document.querySelectorAll('.row');
row.forEach(function(row) {
    row.addEventListener('click', rowClicked);

});

// var count=0
// while (count< myBox.length){
//     // console.log(myBox[count]);
//     count++;
// }

function rowClicked(e) {
    // alert(e.target.textContent);

    // myBox = ['cheese', 'sneeze', 'freeze', 'please'];
    // if (count< myBox.length){
    // console.log(myBox[count]);
    // count++;
// }
    e.target.textContent = 'Hello';

};